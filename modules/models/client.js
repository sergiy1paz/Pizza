export class Client {
    constructor({name, surname, phoneNumber}) {
        this.name = name;
        this.surname = surname;
        this.phoneNumber = phoneNumber;
    }
}

// даний клас був створений з метою збереження данних з форми про клієента, але так як ці дані ніде не використовуються 
// він залишився не реаліззованим 